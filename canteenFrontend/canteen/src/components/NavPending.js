import React from 'react';
import { NavLink } from 'react-router-dom';
const NavPending = () => {
      return (  
            <NavLink to="/Orders/Pending">Pending</NavLink>
      );
}
 
export default NavPending;