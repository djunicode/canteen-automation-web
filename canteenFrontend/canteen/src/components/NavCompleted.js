import React from 'react';
import { NavLink } from 'react-router-dom';
const NavCompleted = () => {
      return (  
              <NavLink to="/Orders/Completed">Completed</NavLink>   
      );
}
 
export default NavCompleted;